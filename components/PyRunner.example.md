# Usage in a slide:

<PyRunner code="print('Hello from Python!')\nfor i in range(5):\n    print(f'  Iteração {i}')" />

# Read-only example:
<PyRunner code="x = 42\nprint(f'A resposta é {x}')" :readOnly="true" />
